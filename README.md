# Mini-Auto-ML-Interactive-Data-Explorer
The application takes ANY preprocessed CSV, although does its best to auto preprocess despite that, and allows you to pick CSV variables. The user may choose ONE independent and ONE dependent variable to plot using to plot using classification or regression (decided by dependent variable choice).
